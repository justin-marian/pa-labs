all: knapsack_fractioner events

knapsack_fractioner: ../knapsack_fractioner.c
	gcc ../knapsack_fractioner.c -o knapsack_fractioner

events: ../events.c
	gcc ../events.c -o events

clean:
	rm -rf knapsack_fractioner events
